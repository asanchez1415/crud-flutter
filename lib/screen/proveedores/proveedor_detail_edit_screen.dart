import 'package:flutter/material.dart';

class ProveedorDetailEditScreen extends StatelessWidget {
  const ProveedorDetailEditScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ProveedorDetailEditScreen'),
      ),
      body: const Center(
        child: Text('ProveedorDetailEditScreen'),
      ),
    );
  }
}
